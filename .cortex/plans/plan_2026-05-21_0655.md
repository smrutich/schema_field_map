---
created: 2026-05-21T06:55:15.436Z
session: 9f3ef635-31f0-426a-ae52-3400e9980ed4
working_directory: /Users/smrutichourasia/Documents/GitHub/Project/schema_field_map
---

## Plan: Merge derive_transformations into map_fields

### Context
- `map_fields` currently produces `field_mappings` and stops
- `derive_transformations` (Step 11) was planned as a separate node but can be folded in since it operates on the just-produced mappings
- `TransformationRule` model already exists in `src/models/schemas.py`
- Need a new prompt template in `src/prompts/__init__.py`

### Implementation Steps

1. **Add prompt to `src/prompts/__init__.py`** — `DERIVE_TRANSFORMATIONS_SYSTEM_PROMPT` and `DERIVE_TRANSFORMATIONS_USER_TEMPLATE`

2. **Add transformation logic to `src/nodes/map_fields.py`**:
   - Add a helper `_needs_llm_transform(fm: FieldMapping, source_lookup) -> bool` that uses rule-based heuristics to detect non-trivial transforms:
     - Source comment contains value codes (e.g., pattern like `X=Value`) → needs LLM
     - Source is integer-like type, dest contains "Boolean" in type_transform → TYPE_CAST
     - Source has PRIMARY KEY constraint and dest is ObjectId → ID_STRATEGY  
     - Source is DATETIME and dest is ISODate → FORMAT_CHANGE
     - Otherwise → NONE (no LLM needed)
   - Add a helper `_assign_trivial_transforms(...)` for rule-based NONE assignments
   - Add a helper `_derive_transformations_llm(...)` that batches non-trivial pairs into 1-2 LLM calls using the mapping client
   - Update `map_fields()` to call these after producing field_mappings, writing `state["transformation_rules"]`

3. **No changes to models** — `TransformationRule` already exists with the needed fields

### Verification
- Run import check: `from src.nodes.map_fields import map_fields`
- Run pipeline through parse→embed→retrieve→map with simulated routing and verify `state["transformation_rules"]` is populated
- Confirm no hardcoded field names — heuristics use constraint/type/comment patterns

### Critical Files
1. `src/nodes/map_fields.py` — main changes (add transform logic)
2. `src/prompts/__init__.py` — new prompt template
3. `src/models/schemas.py` — existing `TransformationRule` (read-only reference)